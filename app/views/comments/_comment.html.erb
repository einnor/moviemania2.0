<%= div_for(comment) do %>

	<p>
		<strong>Posted <%= time_ago_in_words(comment.created_at) %> ago</strong><br />
		<%= h(comment.body) %><br />

		<% if admin_signed_in? %>

			<%= link_to "Destroy", [@cinema, @nowshowing, comment], :confirm => "Are you sure you want to delete this comment?", :method => :delete %>

		<% end %>
	</p>

<% end %>
