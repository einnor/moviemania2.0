<div class="center">
	<div class="panel callout round">
		<h5>Movies Coming Soon</h5>
	</div>
</div>


<div class="row">
	<% @comingsoons.each do |comingsoon| %>
	<div class="large-4 columns">
		<%= image_tag comingsoon.comingsooncover.url(:thumb), :class => "round center" %>
		<h5><%= comingsoon.title %></h5>
		<p><%= comingsoon.category %></p>
		<p><%= comingsoon.cinema.name %></p>
		<p><%= comingsoon.synopsis %></p>
		<!--
			<p>
				<%= link_to "Show", cinema_comingsoon_path(@cinema, comingsoon)%> |
			<%= link_to "Watch Trailer", '#'%> |
			<% if admin_signed_in? %>
				<%= link_to "Edit", edit_cinema_nowshowing_path(@cinema, comingsoon) %>

			<% end %>
			</p>
		-->

	</div>

	<% end %>
</div>
