<div class="row">

	<% @reservations.each do |reservation| %>
	<div class="transactionlist">
		<div class="panel">
			<div class="row">
				<div class="large-4 columns">
					<p><%%= "#{reservation.fname} #{reservation.sname}"%></p>
				</div>
				<div class="large-4 columns">
					<p><%= reservation.email %></p>
				</div>
				<div class="large-4 columns">
					<p><%= reservation.phone %></p>
				</div>
			</div>
		</div>
	</div>
	<% end %>

</div>
