<%= form_for :nowshowing, :html => {:multipart => true} do |f|%>

	<div class="row">
		<div class="large-8 columns">
			<fieldset class="round">
				<legend>New Movie Post</legend>

				<%= f.label :title %> <br />
				<%= f.text_field :title %>

				<%= f.label :synopsis %> <br />
				<%= f.text_area :synopsis %>

				<%= f.label :category %> <br />
				<%= f.text_area :category %>

				<%= f.label :youtubestub %>
				<%= f.text_field :youtubestub %>

				<%= f.label :price %>
				<%= f.text_field :price %>

				<%= f.label :cover %> <br />
				<%= f.file_field:cover %>

				<%= f.submit "Post", :class => "small button radius" %>
			<fieldset>
			</div>
		</div>

<% end %>
