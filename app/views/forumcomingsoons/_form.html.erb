<%= form_for(@forumcomingsoon) do |f| %>
  <% if @forumcomingsoon.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@forumcomingsoon.errors.count, "error") %> prohibited this forumcomingsoon from being saved:</h2>

      <ul>
      <% @forumcomingsoon.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :comingsoon_id %><br>
    <%= f.number_field :comingsoon_id %>
  </div>
  <div class="field">
    <%= f.label :body %><br>
    <%= f.text_area :body %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
