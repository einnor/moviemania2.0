<% @cinemas.each do |cinema| %>

	<div class="panel">
		<div class="row">

  		<div class="small-2 columns">
  			<%= image_tag cinema.cinemalogo.url(:thumb) %>
  		</div>

  		<div class="small-10 columns">
  			<legend><%= cinema.name %></legend><br />
  			<p><%= cinema.description %> </p>
				<%= link_to "Show", cinema %>
				<% if admin_signed_in? %> |
				<%= link_to "Edit", edit_cinema_path(cinema) %> |
				<%= link_to "Destroy", cinema, :confirm => "Are you sure?", :method => :delete %>
				<% end %>

  		</div>
		</div>
	</div>

<% end %>
